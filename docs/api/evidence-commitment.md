# Moved: `evidence-commitment.md` → [`records-commitment.md`](records-commitment.md)

This document was renamed on 2026-08-19 by the vocabulary settlement recorded
in Appendix J of the Typed Standards specification
([civic-ai-tools#160](https://github.com/npstorey/civic-ai-tools/issues/160)).
The artifact this endpoint describes is a **record package**; "evidence" is
retained only for its epistemic sense.

**The contract is unchanged.** `GET /api/records/<hash|slug>/commitment` and its
permanent alias `GET /api/evidence/<hash|slug>/commitment` both serve, and the
sidecar shape documented at the new location applies to either.

→ **[`docs/api/records-commitment.md`](records-commitment.md)**

This stub is permanent. Links to this filename exist in the wild — in issues,
in the specification, and in integrators' notes — and they keep resolving.
